{
  "name": "playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:dev": "set \"ENV=dev\" && set \"CI=true\" && npx playwright test",
    "test:staging": "set \"ENV=staging\" && set \"CI=true\" && npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "playwright": "^1.61.0"
  }
}
